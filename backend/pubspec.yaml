name: beany_backend
description: A server app using the shelf package and Docker.
version: 1.0.0

publish_to: none

environment:
  sdk: ^3.1.3

dependencies:
  args: ^2.3.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  http: ^0.13.0
  beany_core:
    path: ../core
  qr: ^3.0.1
  equatable: ^2.0.5
  quiver: ^3.2.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
